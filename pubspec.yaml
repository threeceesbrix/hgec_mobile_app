name: hgec_mobile_app
description: A new Flutter project.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  device_info_plus: ^10.1.0
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  http: ^1.2.1
  intl: ^0.19.0
  path: ^1.9.0
  path_provider: ^2.1.3
  sqflite: ^2.3.3+1
  stacked: ^3.4.0
  stacked_services: ^1.1.0
  table_calendar: ^3.1.1

dev_dependencies:
  build_runner: ^2.4.5
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  mockito: ^5.4.1
  stacked_generator: ^1.3.3

flutter:
  uses-material-design: true
  assets:
    - assets/
